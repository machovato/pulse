# Environment Variables for Pulse

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth (optional — only needed for public/hosted deployments)
# When running on localhost, Pulse bypasses auth automatically.
# Set this if you deploy Pulse to a public URL and want to protect the editor.
# ADMIN_SECRET=change-me-to-a-strong-secret
